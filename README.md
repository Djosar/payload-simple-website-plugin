# Simple Website Plugin
## Directories
```yaml
dev: # A payload application used for the plugin's development
src: # The plugin's source directory 
  blocks: # The plugin's blocks live here
  collections: # The plugin's collections live here
	media:
	navigations:
	pages:
  fields: # All the plugin's necessary fields live here
  globals: # The plugin's globals live here
```