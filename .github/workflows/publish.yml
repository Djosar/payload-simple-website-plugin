name: Publish

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.0
      - name: Build
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: pnpm i
      - run: pnpm build
